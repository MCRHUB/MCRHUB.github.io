---
import Layout from "../layouts/Layout.astro";
import Pagination from "../components/Pagination.astro";
import type { ShortPageData } from "../shared/type";
import config from "urodele.config";

interface Props {
  posts: ShortPageData[];
  page: number;
  pages: number;
}

const { posts, page, pages } = Astro.props;
---

<script>
  import { mount } from "../components/RelativeTime";
  mount("data-acc-time");
</script>

<Layout title={config.head.title}>
  <!-- 顶部标题区：替换原来的黑块，改为浅色渐变 -->
  <div class="hero-header">
    <h1 class="brand-title">{config.head.brand}</h1>
    <div class="title-line"></div>
    <p class="brand-desc">{config.head.description}</p>
  </div>

  <main>
    <div class="content-wrapper">
      {
        posts.map((item) => (
          <div class="post-card group">
            <a href={`/post/${item.id}`} class="card-link">
              <div class="card-text">
                <h2 class="post-title">{item.title}</h2>
                <p class="post-intro">{item.intro}</p>
              </div>
              {item.cover && (
                <img class="post-cover" src={item.cover.src} alt={item.cover.alt} />
              )}
            </a>
            
            <div class="card-meta">
              <div class="tag-list">
                {item.tags.map((tag) => (
                  <a href={`/tag/${encodeURIComponent(tag)}`} class="tag-pill">
                    #{tag}
                  </a>
                ))}
              </div>
              <div data-acc-time={item.createTime} class="post-date" />
            </div>
          </div>
        ))
      }
      <Pagination page={page} pages={pages} />
    </div>
  </main>
</Layout>

<style>
  /* ===== 顶部标题区样式 ===== */
  .hero-header {
    text-align: center;
    padding: 60px 20px 40px;
    background: linear-gradient(135deg, #f8fafc 0%, #e2e8f0 100%);
    border-bottom: 1px solid #eaeaea;
  }
  .brand-title {
    font-size: 36px;
    font-weight: 800;
    color: #1a1a1a;
    margin: 0 0 12px;
    letter-spacing: -0.02em;
  }
  .title-line {
    width: 48px;
    height: 4px;
    background: #2563eb;
    border-radius: 2px;
    margin: 0 auto 16px;
  }
  .brand-desc {
    font-size: 15px;
    color: #64748b;
    margin: 0;
  }

  /* ===== 文章列表容器 ===== */
  .content-wrapper {
    max-width: 800px;
    margin: 0 auto;
    padding: 32px 24px 60px;
  }

  /* ===== 文章卡片样式 ===== */
  .post-card {
    background: #ffffff;
    border: 1px solid #eaeaea;
    border-radius: 16px;
    padding: 24px;
    margin-bottom: 20px;
    transition: all 0.25s ease;
  }
  .post-card:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 24px rgba(0, 0, 0, 0.06);
    border-color: #dbeafe;
  }

  /* 卡片内部链接与布局 */
  .card-link {
    display: flex;
    justify-content: space-between;
    align-items: flex-start;
    text-decoration: none;
    gap: 20px;
  }
  .card-text {
    flex: 1;
    display: flex;
    flex-direction: column;
    gap: 8px;
  }
  .post-title {
    font-size: 20px;
    font-weight: 700;
    color: #1a1a1a;
    margin: 0;
    line-height: 1.4;
    transition: color 0.2s;
  }
  .post-card:hover .post-title {
    color: #2563eb;
  }
  .post-intro {
    font-size: 14px;
    color: #64748b;
    line-height: 1.6;
    margin: 0;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
  }
  .post-cover {
    width: 120px;
    height: 80px;
    border-radius: 12px;
    object-fit: cover;
    flex-shrink: 0;
  }

  /* 卡片底部信息（标签和日期） */
  .card-meta {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: 16px;
    padding-top: 12px;
    border-top: 1px solid #f1f5f9;
  }
  .tag-list {
    display: flex;
    gap: 8px;
    flex-wrap: wrap;
  }
  .tag-pill {
    background: #eff6ff;
    color: #2563eb;
    font-size: 12px;
    font-weight: 500;
    padding: 3px 10px;
    border-radius: 6px;
    text-decoration: none;
    transition: background 0.2s;
  }
  .tag-pill:hover {
    background: #dbeafe;
  }
  .post-date {
    font-size: 12px;
    color: #94a3b8;
    white-space: nowrap;
  }

  /* ===== 移动端适配 ===== */
  @media (max-width: 640px) {
    .hero-header {
      padding: 40px 16px 24px;
    }
    .brand-title {
      font-size: 28px;
    }
    .content-wrapper {
      padding: 20px 16px 40px;
    }
    .post-card {
      padding: 20px;
    }
    .card-link {
      flex-direction: column-reverse;
      gap: 12px;
    }
    .post-cover {
      width: 100%;
      height: 160px;
    }
  }
</style>
